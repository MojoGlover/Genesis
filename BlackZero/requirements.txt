# BlackZero Agent Requirements
# Pinned versions — do not upgrade without testing

# Core
fastapi>=0.110.0,<1.0
uvicorn>=0.27.0,<1.0
pydantic>=2.0.0,<3.0
pyyaml>=6.0,<7.0

# LangChain / LangGraph
langchain>=0.2.0,<1.0
langchain-community>=0.2.0,<1.0
langgraph>=1.0.0,<2.0
langgraph-checkpoint>=2.1.0,<4.0.0
langgraph-checkpoint-sqlite>=3.0.0,<4.0.0
langchain-ollama>=0.1.0,<1.0

# MCP
mcp>=1.0.0,<2.0

# RAG / Memory — moved to requirements-memory.txt (2026-07-15). Installed only
# when a manifest sets `memory: true` (stamp.py --memory) — see
# agent/modules/rag.py, config.yaml's modules.rag.enabled. Most agents don't
# use semantic recall and were paying a slow torch-class install for nothing.

# PlugOps connection (plugops_bridge uses SSE + HTTP since 2026-05-04)
httpx-sse>=0.4,<1.0

# Utilities
requests>=2.31,<3.0
httpx>=0.27.0,<1.0
