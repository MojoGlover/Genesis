SAFETY POLICY

This file defines the safety constraints for this agent.
Replace placeholder content when instantiating a real agent.

Expected contents:
- A clear list of things this agent will NEVER do
- Categories of requests it will refuse
- How it handles unsafe, ambiguous, or harmful inputs
- Escalation behavior (what happens when a safety boundary is hit)
- Any content filters or output restrictions

This file must remain human-readable and separate from implementation code.
Do not bury safety rules inside source files — they belong here.
