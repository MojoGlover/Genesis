# {AGENT_NAME} environment configuration
# Copy to .env and fill in values. Never commit .env to source control.

# ── Cloud fallback keys (optional — Ollama is primary) ──────────────────────
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# ── Model overrides ──────────────────────────────────────────────────────────
FALLBACK_MODEL_ANTHROPIC=claude-haiku-4-5
FALLBACK_MODEL_OPENAI=gpt-4o-mini

# ── Ollama (local: leave default; Docker: set to sidecar container hostname) ─
OLLAMA_API_URL=http://localhost:11434/api

# ── PlugOps bus (optional — bridge is disabled if unset) ────────────────────
PLUGOPS_URL=
PLUGOPS_AGENT={AGENT_NAME}

# ── Storage (Docker: set to /data and mount a volume) ───────────────────────
DATA_DIR=
