from setuptools import setup, find_packages
setup(
    name="cb_core",
    version="1.0.0",
    description="Computer Black AI core brain — psychology, economy, governance",
    packages=find_packages(),
)
